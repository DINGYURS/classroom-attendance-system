<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.backend.mapper.CourseStudentMapper">

    <resultMap id="BaseResultMap" type="com.project.backend.pojo.entity.CourseStudent">
        <id column="id" property="id"/>
        <result column="course_id" property="courseId"/>
        <result column="student_id" property="studentId"/>
        <result column="join_time" property="joinTime"/>
    </resultMap>

    <select id="findStudentIdsByCourseId" resultType="java.lang.Long">
        SELECT student_id
        FROM course_student
        WHERE course_id = #{courseId}
    </select>

    <select id="findByCourseId" resultMap="BaseResultMap">
        SELECT id, course_id, student_id, join_time
        FROM course_student
        WHERE course_id = #{courseId}
    </select>

    <select id="countByCourseId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM course_student WHERE course_id = #{courseId}
    </select>

    <insert id="batchInsert">
        INSERT INTO course_student (course_id, student_id, join_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.courseId}, #{item.studentId}, #{item.joinTime})
        </foreach>
    </insert>

    <delete id="delete">
        DELETE FROM course_student
        WHERE course_id = #{courseId} AND student_id = #{studentId}
    </delete>

    <delete id="deleteByCourseId">
        DELETE FROM course_student WHERE course_id = #{courseId}
    </delete>

</mapper>
