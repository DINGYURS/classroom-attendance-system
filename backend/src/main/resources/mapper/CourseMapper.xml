<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.backend.mapper.CourseMapper">

    <resultMap id="BaseResultMap" type="com.project.backend.pojo.entity.Course">
        <id column="course_id" property="courseId"/>
        <result column="course_name" property="courseName"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="semester" property="semester"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT course_id, course_name, teacher_id, semester, description, create_time
        FROM course
        WHERE course_id = #{courseId}
    </select>

    <select id="findByTeacherId" resultMap="BaseResultMap">
        SELECT course_id, course_name, teacher_id, semester, description, create_time
        FROM course
        WHERE teacher_id = #{teacherId}
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="courseId">
        INSERT INTO course (course_name, teacher_id, semester, description, create_time)
        VALUES (#{courseName}, #{teacherId}, #{semester}, #{description}, #{createTime})
    </insert>

    <update id="update">
        UPDATE course
        <set>
            <if test="courseName != null">course_name = #{courseName},</if>
            <if test="semester != null">semester = #{semester},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE course_id = #{courseId}
    </update>

    <delete id="deleteById">
        DELETE FROM course WHERE course_id = #{courseId}
    </delete>

</mapper>
